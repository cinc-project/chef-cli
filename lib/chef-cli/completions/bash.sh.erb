_cinc_comp() {
    local COMMANDS="<%= commands.keys.join(' ')-%>"
    COMPREPLY=($(compgen -W "$COMMANDS" -- ${COMP_WORDS[COMP_CWORD]} ))
}
complete -F _cinc_comp <%= habitat? ? "chef-cli" : "cinc" %>
